# Cuter image processors

`image-processors.js` is an unmodified snapshot of the local
`cuter/01/image-processors.js` working file, copied on 2026-09-17.
The source checkout was at `8648bcd` with uncommitted changes to this file;
the checksum identifies the exact snapshot, not that commit alone.

SHA-256: `e72efbf75299a758495cea4813d18e100fc2ff8b0ce36df8ccfaeb46fc4a5ce1`

Vite reads this copy and adapts its UMD export for the frontend. Builds never
search for a sibling checkout or download an updated algorithm. To update the
snapshot, replace this file deliberately, update its provenance and checksum,
and run the frontend tests and build.
